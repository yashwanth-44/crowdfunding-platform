-- Create extensions
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";

-- Note: Prisma migrations will handle table and index creation
-- This file only sets up extensions and grants permissions

-- Grant appropriate permissions (if using separate users)
-- GRANT CONNECT ON DATABASE crowdfunding TO user;
-- GRANT USAGE ON SCHEMA public TO user;
-- GRANT ALL PRIVILEGES ON ALL TABLES IN SCHEMA public TO user;
-- GRANT ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public TO user;
